To compile execute "compile.sh $file_name_without_extension"
It assumes that the file is .cu and needs nvcc on path

To execute run the generated executable in any cuda capable machine with compatible architecture
To reduce arch missmatches, execute on the same machine used to compile.
